---
name: seitai-video-edit
description: 整体プログラム用の動画自動編集。動画を渡すと、冒頭/末尾の自動カット→文字起こし→用語補正→自然な改行のテロップ→焼き込み（1080p）まで一言で実行。さらに扉絵・サイドテロップ・BGM・任意カット・指定語強調まで日本語プロンプトで対応。「この動画にテロップ入れて」「扉絵入れて」「BGMつけて」「サイドテロップ」「◯分◯秒を切って」「◯◯を強調して」等で起動。
---

# 整体プログラム 動画編集スキル

ユーザーの日本語の頼みを、このスキルのスクリプト実行に翻訳する。

**SKILL_DIR = この SKILL.md があるフォルダ**（`~/.claude/skills/seitai-video-edit/` か、cloneした場所）。
実行は必ず SKILL_DIR に `cd` してから。パスはベタ書きせず、この SKILL.md の場所を使う:
```bash
cd "<SKILL_DIR>"          # 例: cd ~/.claude/skills/seitai-video-edit
```
入力動画はユーザーが渡したパス（SKILL_DIR や `downloads/` に置かれることが多い）。YouTube URLを渡されたら `edit_video.py` がそのまま受けて自動DLする。

## 初回セットアップ（`.venv` が無ければ最初に1回だけ自動でやる）

`SKILL_DIR/.venv` が存在しなければ、ユーザーに断ってから次を実行して環境を整える（以降は不要）:
```bash
command -v ffmpeg >/dev/null || brew install ffmpeg          # macOS。Linuxは apt 等
command -v yt-dlp >/dev/null || brew install yt-dlp
uv venv --python 3.12 .venv                                  # uvが無ければ python3 -m venv .venv
.venv/bin/python -m pip install faster-whisper budoux Pillow # uv環境なら uv pip install ...
```
文字起こしモデル（medium 約1.5GB）は初回実行時に自動DLされる。fonts/ とbgm/ はリポジトリ同梱なので追加不要。

## 走り出す前に必ずクオリティを選ばせる（毎回）

`edit_video.py` を実行する前に、**毎回かならず**ユーザーに低／中／高のどれかを選ばせる（`AskUserQuestion` で3択）。ユーザーが最初の依頼文で「高クオリティで」「速くていい＝低で」等とすでに指定している場合のみ、確認を省いてそのまま使う。選択結果を `--quality` で渡す。

| 選択肢 | 渡す | 中身 | 体感 |
|---|---|---|---|
| **低クオリティ**（速い） | `--quality low` | model=small / 720p / crf26 | 速い。誤字が出やすく画質も落ちる。下見・確認用 |
| **中クオリティ**（標準・既定） | `--quality medium` | model=medium / 元解像度or1080p / crf18 | バランス。通常はこれ |
| **高クオリティ**（遅い） | `--quality high` | model=large-v3 / 元解像度or1080p / crf16 / preset slow | 誤変換が激減し画質も最良。時間がかかる |

注意: この環境はCPUのみで、文字起こしが一番のボトルネック。中・高は数分〜十数分かかることがある。先にユーザーへ「中・高は時間がかかる」と一言添えてから選ばせる。VADを効かせると無音・非発話区間で文字起こしが暴走する事故を防げて速くなるので、基本は `--vad` を併用する。

## プロンプト → コマンド 対応

すべて venv の python（`.venv/bin/python`）で実行する。出力は入力と同じ場所。**実行コマンドには必ず選ばせた `--quality <low|medium|high>` を付ける。**

| ユーザーの言い方 | 実行 |
|---|---|
| 「テロップ入れて」「字幕つけて」「編集して」(基礎) | `edit_video.py "<動画>" --burn` |
| ＋「扉絵も入れて」「章の区切りに見出し」 | 上記に `--titles` |
| ＋「右上にサイドテロップ」「章名を出して」 | 上記に `--side` |
| ＋「BGMつけて（おまかせ）」 | 上記に `--bgm auto` |
| ＋「BGMは pop／chill／piano／ambient で」 | `--bgm pop`（bgm/の名前で部分一致） |
| ＋「BGMは自分の◯◯.mp3で」 | `--bgm "<パス>"` |
| 「"軽擦"と"血行"を強調して」 | `edit_video.py "<動画>" --burn --emphasis 軽擦,血行`（他の付与も併記で再実行） |
| 「1:20〜1:35 と 2:40〜2:50 を切って」 | `cut_ranges.py "<完成動画>" "<出力>" 1:20-1:35 2:40-2:50` |
| 「もっと精度よく」 | `--quality high`（large-v3。遅いが誤変換激減） |
| 「速く試したい」 | `--quality low`（small。速いが誤字でやすい） |

全部入りの例（クオリティは選ばせた値を入れる）:
```bash
.venv/bin/python edit_video.py "<動画>" --quality medium --vad --burn --titles --side --bgm pop
```
出力の最終ファイルはログの「★完成:」行（`<名前>_final.mp4` 等）。

## 各機能の中身

- **クオリティ（`--quality`）**: low/medium/high で「文字起こしモデル＋出力解像度＋画質(crf/preset)」をまとめて切替。既定 medium。詳細は上の表。`--model` を明示すると `--quality` のモデルだけ上書きできる。
- **基礎（テロップ）**: 冒頭/末尾の自動カット（既定ON・発話の1.5秒手前〜）→ faster-whisper文字起こし → `用語辞書.txt`で補正 → BudouXで自然改行（1行16字・2行・句点なし）→ Noto Sans JP Black で焼き込み、**出力解像度へ拡大しテロップを高解像度描画＋libx264（crfはクオリティ依存）**。テロップの動きは既定なし（`--pop beats`で区切りだけ動かせる）。強調は既定なし（`--emphasis`指定時のみ）。
- **扉絵（`--titles`）**: 章の頭フレームをぼかし＋暗くした背景＋見出し。クロスフェードで挿入。章立ては `<名前>.章立て.txt` を自動生成（「N番目」等を検出）。ユーザーが直したい場合はこのファイルを編集して再実行（時刻・見出し・`img=`/`full=`指定可、`[OP]`で冒頭扉絵）。
- **サイドテロップ（`--side`）**: 右上に章名を常駐（白い長方形カード＋影＋縁取り文字）。章立てに連動。
- **BGM（`--bgm`）**: `bgm/`内のCC0音源 or 持ち込み。小音量＋ナレーション中は自動で下がる（ダッキング）＋ループ＋終端フェード。`bgm/CREDITS.md`にライセンス記録。
- **カット（`cut_ranges.py`）**: 完成動画から指定区間を削除して連結。
- **強調（`--emphasis 語,語`）**: 指定語だけテロップで黄色に。数字の自動強調は止める。

## 誤字をまとめて直す（校正）＝ユーザーの手間を増やさない

誤字が多いとき、ユーザーに1個ずつ指摘させない。**Claudeが全文を読んで一括校正する。**
「誤字直して」「誤字多い」等と言われたら（個別指摘が無くても）次をやる:

1. `<名前>.txt`（補正済み全文）を読み、整体の知識で**誤変換を全部洗い出す**（軽擦/施術/揉捏/叩打/振戦/仙腸関節…や文脈上おかしい語）。
2. **繰り返す固有語**は `用語辞書.txt` に `誤り=正しい`（文脈依存なら長めの句で）を**まとめて追記**。
3. `edit_video.py` を同じ引数で**再実行**（キャッシュで文字起こしはスキップ、辞書適用＋焼き直しだけ＝速い）。
4. 辞書で拾えない一回限りの誤りは、`<名前>.srt` を直接直して `burn_anim.py "<動画>" "<直したSRT>" "<出力>" --style=yellow` で焼き直す（必要なら titlecard / add_bgm を続けて適用）。

→ ユーザーは結果をざっと確認するだけ。探す・指示する作業はClaude側。

**誤字の母数を元から減らす:** 精度が要るときは `--model large-v3`（mediumより誤変換が激減。遅いがキャッシュで一度きり）。「誤字が多い」と言われたらまず large-v3 での再実行を提案する。

## 専門用語の補正（精度の肝）

Whisperが整体用語を誤る（軽擦→「警察/検察」、施術→「主義」等）。`用語辞書.txt` に `誤り=正しい` を1行足すと次回から直る。キャッシュが効くので再文字起こしは走らず一瞬で反映。

## 文字起こしキャッシュ

`.cache/` に保存（キー＝動画×モデル×VAD）。同じ動画・モデルなら2回目以降は文字起こしをスキップ。用語辞書・強調・扉絵などの調整が一瞬で回る。強制やり直しは `--refresh`。

## 配布物メモ（GitHub公開）

- フォント `fonts/NotoSansJP-VF.ttf` は SIL OFL 1.1（再配布可。`fonts/OFL.txt` を同梱）
- BGM `bgm/*.mp3` は CC0（`bgm/CREDITS.md` 参照）
- `downloads/`（著作権物のテストDL）・`.venv/`・`.cache/`・中間mp4 は `.gitignore` で除外
